/**
 * Legacy entrypoint — list/editor live in ObjectList / ObjectEditor.
 * Kept so any old imports don't break.
 */
export { ObjectList as ObjectsBrowser } from "./ObjectList";
export { ObjectEditor } from "./ObjectEditor";
